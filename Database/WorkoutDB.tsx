"use strict";

import { SQLiteDatabase } from "expo-sqlite";
import { Alert } from "react-native";

const insertWorkoutA = (db:SQLiteDatabase, workout: Workout) => {
    Alert.alert("TODO make functions to insert workouts on button press")
}