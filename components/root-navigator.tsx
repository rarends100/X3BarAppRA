/*import { isAdminContext } from "@/context/RA_user-Auth-context";
import { useContext } from "react";

import { isLoggedInContext } from "@/context/RA_user-Auth-context";
import { isTraineeContext } from "@/context/RA_user-Auth-context";

import {createNativeStackNavigator} from '@react-navigation/native-stack';

const RootNavigator = () => {
  const isAdmin = useContext(isAdminContext);
  const isLoggedIn = useContext(isLoggedInContext);
  const isTrainee = useContext(isTraineeContext);
  
  const Stack = createNativeStackNavigator();
  
  return (
   
  );
};*/